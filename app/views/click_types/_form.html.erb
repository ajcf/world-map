<%= form_for(@click_type) do |f| %>
  <% if @click_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@click_type.errors.count, "error") %> prohibited this click_type from being saved:</h2>

      <ul>
      <% @click_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
